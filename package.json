{
  "name": "pulumi-notes-app",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "preup": "npm run build"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "@types/node": "^20.0.0",
    "@types/aws-lambda": "^8.10.133"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.826.0",
    "@aws-sdk/lib-dynamodb": "^3.826.0",
    "aws-lambda": "^1.0.7",
    "@pulumi/pulumi": "^3.0.0",
    "@pulumi/aws": "^6.0.0"
  }
}
